#include <stdio.h>
/**
 * main - A function that prints string using puts()
 * Return: 0(success)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
